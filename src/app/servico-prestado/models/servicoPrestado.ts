import { Cliente } from "src/app/clientes/models/cliente";

export class ServicoPrestado{
  nome: string;
  plano: string;
  dataAtividade: string;
  dataCadastro: string;
  duracao: string;
  hora: string;
  nota: number;
  observacao: string;
  responsavel: string;
  valor: number;
  idCliente: number;
}
